import Vue from 'vue';
import Vuetify from 'vuetify/lib/framework';

Vue.use(Vuetify);

export default new Vuetify({
    themes: {
        theme: {
            light: {
                primary: "#4436FD"
            }
        }
    }
});
